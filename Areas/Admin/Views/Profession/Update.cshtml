@using WebApplication20.Models
@model Profession

<form asp-controller="Profession" asp-action="Update" method="post" enctype="multipart/form-data">
    <div class="mb-3">
        <label for="Name" class="form-label">Name</label>
        <input asp-for="@Model.Name" type="text" class="form-control" id="name">
        <span asp-validation-for="@Model.Name">Name is required</span>
    </div>
    <button type="submit" class="btn btn-primary">Add</button>
</form>
